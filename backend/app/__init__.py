"""Application package.

The current runtime keeps the original Flask UI intact while exposing a
modular package layout for gradually moving routes, services, repositories,
and AI processing out of the legacy files.
"""
